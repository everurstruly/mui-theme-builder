import { Typography } from "@mui/material";

export default function CanvasBoardFrames() {
  return (
    <Typography variant="h4" sx={{ p: 4, width: 600, height: 800 }}>
      Canvas Board Frames
    </Typography>
  );
}
